# simple_network_generator_tool

Simple Network Generator Tool is a python tool designed to pick some random values for network devices and endpoints and format a YAML configuration file to be read by the LUDUS software. LUDUS can then create a network of VMs based on that config file. 
